# Copy to .env and fill in. For Render: set these in Dashboard → Environment.

PORT=3001
NODE_ENV=production

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.xxxxx.mongodb.net/DATABASE?retryWrites=true&w=majority

# JWT secret for auth tokens
JWT_SECRET=your-secure-random-secret

# Frontend URL for CORS (your Vercel app URL, no trailing slash)
# Multiple origins: comma-separated (e.g. https://app.vercel.app,https://preview.vercel.app)
FRONTEND_URL=https://your-app.vercel.app
